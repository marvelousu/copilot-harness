---
applyTo: "**/*.{c,h}"
---

- 割込みと主処理で共有する変数には `volatile` を付ける。最適化で読み飛ばされる。
- 共有データの読み書きはクリティカルセクションで囲む。部分更新が見える窓を作らない。
- ISR の中でブロッキング処理と動的確保をしない。
- スタック使用量に影響する変更を指摘する。大きなローカル配列と再帰が対象。
- 再現しないバグはコンパイラ最適化と `volatile` 漏れを最初に疑う。
