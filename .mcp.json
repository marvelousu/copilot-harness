{
  "$comment": "テンプレート。Copilot CLI はこのファイルを読まない (1.0.83 で実測)。~/.copilot/mcp-config.json にコピーして使う。github を含めないのは、CLI に github-mcp-server が内蔵されており、ここに書くと既定の絞り込みが '全ツール' に上書きされるため。詳細は docs/verified-on-cli.md。",
  "mcpServers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp"
    },
    "memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "env": { "MEMORY_FILE_PATH": "./.copilot-memory.jsonl" }
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chrome"]
    }
  }
}
